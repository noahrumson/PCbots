to compile c:
g++ -I -fpic -c [files.cpp] -o [files.o]
g++ -shared -o lib[xxx].so [files.o] -lpigpio -lrt

in python:
import ctypes
import os
lib = ctypes.cdll.LoadLibrary(os.path.abspath(libfile))
lib.foo.restype = ctypes.c_[int, double, etc]
lib.foo()
